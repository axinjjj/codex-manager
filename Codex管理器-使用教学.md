# Codex 管理器 · 小白教学

## 一、这是什么？

Codex 用久了会**在你电脑里偷偷堆垃圾**——几百上千条 md 文件、测试残留、缓存副本……
而且它的配置文件（md 和 skill）藏得很深，你平时根本看不见，更别说管了。

这个工具就干两件事：

1. **看得见** —— 把你 Codex 的配置、技能、记忆全部可视化，分类摆好，
   每个文件是干嘛的都用人话标注，不用一层层钻文件夹
2. **管得住** —— 垃圾一键清理、文件随便看/改/删、
   删错了有回收站（7 天内随便捞；再次打开回收站时，过期内容会自动清理）

## 二、安装（30 秒）

### Windows

1. 装 Python 3：去 python.org 下载，安装时**勾选 "Add python.exe to PATH"**
2. 把「Codex管理器」文件夹解压到随便什么位置
3. 双击文件夹里的 **「① 双击我安装.bat」** → 桌面出现两个图标，搞定

### macOS

1. 打开终端运行 `python3 --version`，确认已经安装 Python 3
2. 把「Codex管理器」文件夹解压到随便什么位置
3. 双击文件夹里的 **「打开 Codex 管理器-macOS.command」** 或 **「打开 Codex 库存仪表盘-macOS.command」**

macOS 管理器默认是**完整管理模式**，和 Windows 一样支持编辑、删除、恢复、一键清理和新建 Skill。删除及整组清理前会弹出确认，内容先进入隔离区；直接编辑保存不会自动备份，请确认内容后再保存。第一次打开若被系统拦截，在 Finder 里右键该 `.command` 文件，再选择「打开」。

## 三、使用

### 🔧 Codex 管理器（主工具）

| 区域 | 干什么 |
|---|---|
| 文件管理 | 左边分组：配置文件 → Skills → 记忆系统 → 临时垃圾 → 历史记录 → 系统托管。点组名展开，点文件夹原地展开，点文件右边看内容 |
| 每个文件 | 可以直接**编辑**（保存即生效）、**删除**（进回收站）、**🌐 翻译成中文**（需要本机有 AI 代理，没有就用不了，其他功能不受影响） |
| md 查重 | 一键找出内容重复的 md，重复的直接删 |
| 新建 Skill | 填个名字点保存，模板自动生成，格式错了会红字提醒 |
| 🗑 回收站 | 删掉的东西都在这，可以**恢复**回原位；每次打开回收站时，超过 **7 天**的内容会自动彻底删除 |

### 📊 Codex 库存仪表盘（体检报告）

双击 → 自动扫描 → 看最上面的灯：
- 🟢 绿灯 = 干净，关掉该干嘛干嘛
- 🔴 红灯 = 垃圾超标，打开管理器清理

macOS 版报告由同一个 Python 管理器生成，只读取 `~/.codex`，HTML 放在系统临时目录。想先看测试副本，可以在终端运行：

```bash
python3 "Codex管理器/零件箱/Codex配置管理器.py" --root "/你的测试目录/.codex" --inventory
```

如果只想查看、不允许任何编辑或清理，可以启动只读模式：

```bash
python3 "Codex管理器/零件箱/Codex配置管理器.py" --read-only
```

## 四、堵根源：让 Codex 别再乱丢垃圾

工具是扫帚，但更重要的是**别让 Codex 继续制造垃圾**。
把下面这段**复制进你的 AGENTS.md**（没有就新建一个，放在项目根目录或 ~/.codex 下）：

```
## 文件卫生规则
- 临时文件（测试脚本、草稿、一次性产物）只准写到系统临时目录
  （Windows: %TEMP%，Linux/Mac: /tmp），禁止写进项目目录和 ~/.codex。
- 任务收尾前必须删掉所有非交付物（测试文件、中间产物、调试脚本），
  只留下用户明确要求的交付文件。
- 除非用户明确要求，禁止在项目里生成总结/报告类的 .md 文件。
- commit 前先 git status 自查，确认没有混入临时文件。
```

加上之后 Codex 乱造垃圾会明显收敛。
**口诀：AGENTS.md 管源头，管理器管日常，仪表盘管监督。**
