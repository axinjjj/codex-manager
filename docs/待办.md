# 待办

## 1. 翻译缓存

现状：每次点翻译都实时调用 10100 代理，等很久。
需求：翻译过的内容按原文指纹缓存到本地（比如 json 文件）；再翻译同样内容直接用缓存秒出；原文变了才重新翻译。
验收：同一个文件第二次翻译秒开；改过的文件会重新翻译；缓存在翻译不可用时（10100 没开）也能用旧结果。

## 2. skill 单独删除按钮

现状：skill 只能浏览和新建，不能单独删除。
需求：「你的 Skills」列表里每个 skill 旁边加删除按钮，走现成的 `/api/delete` 进隔离区（删错能捞回）。
验收：点删除 -> 该 skill 从列表消失并进入隔离区；隔离区里能恢复。

做完一条就删掉一条，并在 docs/HANDOFF.md 里记一笔。
